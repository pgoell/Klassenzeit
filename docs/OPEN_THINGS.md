# Open Things

Running log of items deferred or noted as tech debt during spec/plan work. The top section is the active sprint; completed sprints sit below it; the general backlog is everything else, grouped by concern. Within each group, items are ordered by importance.

Items trace back to the specs that introduced them: the [project scaffolding design](specs/2026-04-11-project-scaffolding-design.md), the [frontend theming / i18n / ratchet design](specs/2026-04-17-frontend-theming-i18n-design.md), the [entity CRUD pages batch 1 design](specs/2026-04-17-frontend-entity-crud-pages-design.md), and the [frontend design implementation](specs/2026-04-19-frontend-design-implementation-design.md).

## Active sprint program: Solver feasibility correctness + observability

Next pickup: P1 item 34 (backend tidy phase: backend-aware `solve_deadline_ms_by_backend` so cpsat is no longer cut off at the LAHC-tuned 200 ms; now an unblocked candidate since item 48 ports the cpsat objective so giving cpsat more wall-clock pays off) or P1 item 47 (production-default revisit, ADR at minimum 0036 since 0035 is now the Timefold-rejection ADR; fully unblocked since item 44's 2026-05-08 refresh shipped — `solver/solver-core/benches/BENCH_RESULTS.md` is the data ADR 0036 reasons from; headline finding: dreizuegig/cpsat 0/20 feasible at 60 s post-item-48, LAHC family 20/20 at soft_med 1493-1507). Item 21 shipped on 2026-05-07: `RR_K` promoted to `SolveConfig.lahc_rr_k: u32` (default `5`), `solver-bench` gains `--rr-k` / `--rr-period` / `--append` / `--backends` flags with cross-product expansion over `lahc_rr` and `lahc_rr_kempe` cells (non-RR backends render `-` sentinels), `## Pareto frontier` per fixture and a `## Recommendation` block; sweep at `--budget 5s --seeds 4 --fixtures grundschule,zweizuegig,dreizuegig --rr-k 3,5,8 --rr-period 10,25,50` selected `(K=5, period=50)` over the prior `(K=5, period=25)` (mean soft-score median 578 vs 579 across both RR backends; tie-break smallest period after smallest K); bench dispatch table in `solver-bench/src/main.rs` flipped `LahcRr` and `LahcRrKempe` to `Some(50u32)`, K stays at 5 via `SolveConfig::default().lahc_rr_k`. Item 22 shipped on 2026-05-07: `BenchBackend::LahcKempe` standalone bench column dispatches `(lahc_rr_period: None, lahc_kempe_period: Some(23))` matching `LahcRrKempe`'s Kempe period for clean isolation; `solver_core::quality::build_backend_objectives` registers the `lahc_kempe` row between `lahc_rr` and `lahc_rr_kempe` cloning the LAHC-family `optimised: ALL` / `declared_skipped: empty` shape; pre-merge smoke at `--budget 5s --seeds 4 --fixtures grundschule,zweizuegig` confirmed all five backends render without panic, but the home_room collapse hypothesis is untestable at smoke shape (every LAHC variant reads `worst_home_med = 1.00`) and the directional read defers to item 44's full refresh. Item 12 shipped on 2026-05-07: `Subject.prefer_late_period=5` activated for FÖ in the shared `_SUBJECTS` tuple (cascades to all three Python demo seeds via the existing import contract); also fixed a latent bug where `demo_grundschule_zweizuegig.py` and `demo_grundschule_dreizuegig.py` iterated `_SUBJECTS` but their `Subject(...)` constructors omitted the `prefer_late_period` kwarg; mirrored to `zweizuegig_fixture` and `dreizuegig_fixture` in solver-core test fixtures; smoke `bench:bakeoff` shows `late_med=1.00` on zweizuegig across all three LAHC backends. Item 15 shipped on 2026-05-07: criterion bench cfg switched from `class_gap=1, teacher_gap=1` to `PRODUCTION_ACTIVE_WEIGHTS`, fixing the `solver_greedy/zweizuegig` `NoSuitableRoom` panic on lesson 66 (4b SP) under the same-room hard constraint; `BASELINE.md` refreshed; greedy timings 5-8x higher (production weights compute more cost axes per `try_place_block` call) but LAHC stays deadline-bound. Item 54 shipped on 2026-05-07 (class-day-balance search-time objective). Item 48 shipped on 2026-05-07: CP-SAT's `model.minimize(...)` now mirrors `score_solution(..., PRODUCTION_ACTIVE_WEIGHTS)` exactly via per-anchor constant coefficients (subject_preference plus home_room) plus per-(entity, day, position) `present`/`has_left`/`has_right`/`gap` channeling for class_gap and teacher_gap plus per-class `add_abs_equality` plus `add_division_equality` chain for class_day_balance; cpsat soft_score on the grundschule pre-merge smoke drops from the ~349 master baseline to 32; BackendObjective declaration flipped so cpsat declares `optimised: ALL`. Item 52 shipped on 2026-05-07: LAHC accepts, exits, and probes `time_to_optimal_ms` on the canonical objective via the new `state.canonical_score` field; `lahc::run` snapshots `best_placements` at loop entry and restores on exit so `solution.soft_score <= greedy_solution.soft_score`; backend objective declarations widened so `lahc` / `lahc_rr` / `lahc_rr_kempe` declare every `QualityComponent` in `optimised`. Item 51 shipped on 2026-05-07: `solver_core::{QualityComponent, BackendObjective, backend_objective(name)}` plus the `solve_with_config_stats` debug_assert pin every Rust backend's reported `solution.soft_score` against `score_solution`. Item 50 shipped on 2026-05-06: `solver_core::QualityReport` (eleven-field cost-vector breakdown) plus `quality_report(...)` is the single source of truth for cross-backend comparison; `solver-bench` renders four load-bearing component columns; backend `quality_checks.py` documents the per-`QualityIssue.kind` mapping. The LAHC hot path keeps the slice scorer; bench / ADR comparisons consume the canonical vector. Item 32 shipped on 2026-05-06: three sibling solvability tests (`test_seeded_grundschule_*_solves_with_auto_assigned_teachers`) drive the production route flow on einzügig, zweizügig, and dreizügig demo seeds without the canonical `_TEACHER_ASSIGNMENTS_*` pin. All three currently `xfail(strict=False)` because the auto-assigned distribution + lahc_rr_kempe at 5000 ms intermittently surfaces FFD lock-ins; XPASS often, but not deterministically. Tracked under item 11 (einzügig), item 4 (root cause: subject UUID order leak in scarcity-first auto-assign tiebreak). Item 31 shipped on 2026-05-06: five new quality columns plus a composite `Quality (pass / 4)` count in `BENCH_RESULTS.md` (worst spread, worst home-room ratio, total interior gaps, late-period ratio), evaluator lives in `solver/solver-bench/src/quality.rs` with thresholds matching the Python `test_grundschule_schedule_meets_quality_bar` integration test verbatim; home-room exemption is inferred from `Problem.room_subject_suitabilities` rather than hand-supplied; late-period predicate uses `Subject.prefer_late_period > 0` as the proxy and renders `-` when no fixture subject has the axis enabled (today every Rust-side fixture; lights up automatically when item 12 lands). Item 30 shipped earlier on 2026-05-06: per-cell subprocess isolation, ADR 0034. Item 41 shipped earlier: LAHC and cpsat bake-off arms now both push `score_solution(problem, placements, &PRODUCTION_ACTIVE_WEIGHTS)` end-to-end, so cross-backend `soft_score_median` cells compare on the same objective. Item 40 shipped in PR #190; the Kempe chain code's multi-position window walk now has CI coverage via the widened proptest generator, with `validate_no_double_booking`'s `cfg(debug_assertions)` panic catching any future regression on every property-test invocation. Item 39 shipped the post-condition validator trio plus the lesson-group exemption.

Goal: (a) capture peak RAM, time-to-first-feasible, and time-to-optimal per backend so production-default decisions account for real cost, (b) judge produced plans on schedule quality (gaps, spread, home-room ratio) not just hard-violation counts, (c) close the test realism gap so the production-route flow is exercised on the demo seeds, (d) close the silent-hard-violation gap so any move that produces an illegal schedule fails loudly. Phase ordering: correctness gates everything (observability of broken numbers is worse than no observability), then observability, then test realism, then backend tidy.

### Backend tidy phase

34. **Backend-aware `solve_deadline_ms`.** `[P1]` `Settings.solve_deadline_ms: int | None = 5000` (per ADR 0033) is the same wall-clock budget for every backend. 5000 ms is comfortable for the LAHC family on every fixture (item 44's 2026-05-08 production refresh shows LAHC plateau within a few seconds across all four fixtures, `tto_med` 1-4 s on dreizuegig at 60 s budget) and **insufficient for `cpsat` on the largest fixture** (the same refresh shows `dreizuegig/cpsat 0/20 feasible` at 60 s post-item-48). Replace the scalar with `solve_deadline_ms_by_backend: dict[str, int]` (or per-backend env vars `KZ_SOLVE_DEADLINE_MS_LAHC=5000`, `KZ_SOLVE_DEADLINE_MS_CPSAT=120000`), thread through `run_solve` in `solver_io.py:404-429`. Item 30 + item 44's bench data are in; the per-backend deadline is the next backend-tidy lever and could plausibly recover cpsat's dreizuegig feasibility at a longer budget.

## Next sprint program (paused): Schwimmen + Sek-I foundations

Paused 2026-05-05 in favour of the solver-correctness sprint above. Resumes once items 26-32 ship and `BENCH_RESULTS.md` carries the corrected feasibility column. The reasoning: a P0 production-feasibility regression supersedes new-feature foundations, and the ADR 0031 revisit (item 29) may move the production default, which would change the constraints on Schwimmen-buffered placements.

Goal: ship two foundations that unlock the Beyond-Grundschule sprint sequence: (1) Schwimmunterricht modelling (`Room.is_external` flag plus per-Lesson travel buffers, with a Klasse 3 Schwimmen Doppelstunde landing in the dreizügige Grundschule fixture) and (2) further `school_type` consumers as Sek-I fixtures land. Tiers reflect drop-order if the sprint runs long. Phase ordering: schema prereqs first, then solver, then the seed and tidy mix.

### Schema phase

1. **`Room.is_external: bool` flag + per-Lesson travel buffer fields.** `[P0]` Add `Room.is_external: bool = False` (server default false) and per-`Lesson` `pre_buffer_minutes: int | None`, `post_buffer_minutes: int | None` (both nullable, default null). Pydantic / Zod / frontend mirrors. Solver consumes the buffers in the next phase.

### Algorithm phase

2. **Schwimmunterricht solver constraint.** `[P0]` `solve_with_config` rejects placements where the lesson's class set or teacher is already booked in the time-block(s) blocked by `pre_buffer_minutes` (preceding) or `post_buffer_minutes` (following). The buffer is enforced per-class and per-teacher; rooms are unaffected (the external room is the auswärtige Schwimmhalle). New `ViolationKind::TravelBufferConflict` records the affected (class | teacher, time-block) pair. Bench: extend the dreizügige fixture with one Klasse 3 Schwimmen lesson and refresh `BASELINE.md` if the criterion drift exceeds the 3 percent refresh threshold.

### Seed phase

3. **Klasse 3 Schwimmen lesson in `demo_grundschule_dreizuegig`.** `[P0]` One Doppelstunde Schwimmen per Klasse-3 Klasse, pinned to a new external Schwimmhalle Room (`is_external=True`), with `pre_buffer_minutes=15` and `post_buffer_minutes=15` (Hessen Wegezeit Mittelwert). Teacher allocation pinned via `_TEACHER_ASSIGNMENTS_DREIZUEGIG`. Solvability test asserts the buffered slots stay free and `body["violations"] == []`. Per-sprint research: confirm Hessen Klasse 3 Schwimmen is one Doppelstunde / Woche and 10 to 15 min Wegezeit each way (already noted in OPEN_THINGS Hessen Grundschule reference data).

### Tidy phase

4. **Einzügige solvability test transient flakiness.** `[P1]` Investigate only if the flake reproduces on master CI during this sprint; otherwise leaves as the existing deferral. Root-cause is suspected nondeterminism in `scheduling/teacher_assignment.py:auto_assign_teachers_for_lessons` under full-suite ordering (subject UUID order leaks into the heuristic's tiebreak).
5. **Audit toast error fallbacks for copy reuse.** `[P1]` Several `toast.error(err instanceof Error ? err.message : t("x.action"))` call sites use the button-label key as the error fallback (rendering "Save availability" as an error string). PR #116 fixed two known sites; this sweep adds a `mutationErrorKey(entity, action)` helper that returns the right key by convention, then migrates every remaining call site identified by `rg "toast.error\(.*t\(.*\.save"`.

### Drop tier (P2)

6. **Block-aware FFD eligibility for buffered lessons.** `[P2]` Only land if the dreizügige fixture surfaces a real placement-rate regression on the new buffered Schwimmen lesson; otherwise leaves the existing deferral untouched. The buffered lesson is more constrained than a same-eligibility unbuffered lesson because its surrounding time-blocks must also be free for the class and teacher; folding contiguity into the FFD eligibility computation removes the pressure naturally.

## Open solver follow-ups

Quality and tuning items deferred from the bake-off program. Ordered by execution sequence: top items unlock, precede, or correct the data the rest depend on. Priority tags `[P0]/[P1]/[P2]` are kept inline so the reader can still see where each item sits on the importance ladder; the order is no longer purely priority-sorted.

11. **Remove the `xfail` from `test_seeded_grundschule_solves_with_auto_assigned_teachers`.** `[P0]` The test drives the production HTTP route flow on the einzügig demo seed at the production 5000 ms LAHC deadline (ADR 0033) without the canonical `_TEACHER_ASSIGNMENTS_*` pin. Acceptance: 20 of 20 consecutive runs without `pytest.mark.xfail`. The flake is FFD lock-in driven, not LAHC-plateau driven, exactly the risk documented in `solver/CLAUDE.md` under "Adding a placement-time hard constraint can flake FFD greedy without LAHC". Tracked alongside item 4 (suspected subject UUID order leak in scarcity-first auto-assign tiebreak as the upstream root cause). Re-measure at master tip (post-items 21 / 44; the production HTTP route's `lahc_rr_kempe` benefits from the (K=5, period=50) tuning only at the bench dispatch today — production solver-py uses `solve_json_with_config` without an `rr_period` knob, so the post-tuning win is `lahc_rr_period: None` callers; the production fix path needs `Settings.lahc_rr_period: int | None` plumbing if the einzügig flake survives the re-measurement).
14. **Tighten the Grundschule schedule quality bar (remove second `xfail`).** `[P0]` Drop `pytest.mark.xfail` from `test_grundschule_schedule_quality_meets_quality_bar`. Thresholds (`max_spread=2`, `min_ratio=0.6`, `max_gaps_per_class=2`, `max_position=6`). The 2026-05-08 production refresh (item 44) shows grundschule passes `quality 4/4` on every backend at 60 s LAHC budget (`worst_spread=1`, `worst_home_med=1.00`, `gaps=0`); the pytest test runs at the production 5000 ms LAHC deadline (ADR 0033), so the test should now pass consistently. Re-measure with the flake-loop pattern documented in `.claude/CLAUDE.md` (run 20-of-20, treat XPASS as PASS, the question is "does it ever go RED"). If the test still flakes, the FFD lock-in path documented under item 11 is the likely cause; otherwise drop the `xfail` and ship.
47. **ADR 0032 production-default revisit (item 42 follow-up).** `[P1]` Land last in this section. **Do not flip the production default off the 2026-05-06 numbers**. Those numbers were partly an artefact of the bug item 48 (CP-SAT optimises a different objective from solver-core's scorer) has now fixed. Reference data on master tip `040de31` for context: at `--budget 60s --seeds 20`, every backend is 20/20 feasible on every fixture (CP-SAT was 0/4 dreizuegig and 1/4 zweizuegig in the prior 5s shape demo), so the hard gate eliminates nothing, and against the pre-fix backends plain `lahc` ranks first on every fixture (grundschule 90 vs `lahc_rr_kempe` 107; zweizuegig 775 vs 1108; dreizuegig 2235 vs 2414; lock_in 588 vs 606). Item 21 has now shipped (2026-05-07: `(K=5, period=50)` is the new bench dispatch tuning); item 44 has shipped (2026-05-08: production-shape refresh in `solver/solver-core/benches/BENCH_RESULTS.md`). Headline 2026-05-08 finding: post-item-48 CP-SAT loses dreizügig feasibility at 60 s (0/20 vs the LAHC family's 20/20 at soft_med 1493-1507); on zweizuegig CP-SAT is 20/20 feasible but soft_med 1135 vs the LAHC family's 199-241; on grundschule all five backends are 4/4 quality. The LAHC family wins definitively on the multi-school axis. Open the next available ADR (always `ls docs/adr/*.md | sort | tail -1` before assigning; ADR 0035 is the Timefold-rejection ADR landed in item 55, so item 47's ADR is at minimum 0036), re-apply the ADR 0029 decision rule against the 2026-05-08 table, and if the verdict flips the production default, update `Settings.solver_backend` (`backend/src/klassenzeit_backend/core/settings.py`), the lockstep test (`backend/tests/core/test_settings.py`), and the cross-references in `solver/CLAUDE.md` and `backend/CLAUDE.md` in one atomic commit. The new ADR must reason from per-component vectors per the rule landed in solver/CLAUDE.md under item 51, and must explicitly call out worst_spread and worst-class interior gaps as not-yet-optimised quality bars (item 57 is the planned closure).
57. **Widen the canonical objective to bound per-class worst-case quality bars.** `[P1]` Land after item 14 (Grundschule quality bar tightening). The 2026-05-08 production-shape refresh shows the gap: every grundschule cell passes `quality_med=4/4`, but multi-school cells miss because the search-time slice and the canonical accept-time score both push down totals, while the bench predicates measure the worst class. Specifically the bench `worst_spread` predicate (`max(class.lessons_per_day) - min(class.lessons_per_day)` for the worst class) is not in any backend's optimised set today. `class_day_balance` (sum of |actual - mean| per class per day) is the closest in-objective proxy and pulls toward even distributions but does not bound worst-case spread. The bench `total_interior_gaps` predicate measures the worst class's gap count, while `class_gap_hours` in `score_solution` sums across classes, so a "one bad class plus several green classes" solution scores fine on the canonical but fails the predicate. Concrete data points from 2026-05-08 production refresh: zweizuegig/lahc soft_med=241 spread=2 gaps=0 quality 4/4 (hits because LAHC's class_day_balance pull happens to bound spread to 2); zweizuegig/cpsat soft_med=1135 spread=5 gaps=34 quality 2/4 (CP-SAT's item-48 channeling for class_gap is per-(entity, day, position) sum, not per-class max); dreizuegig/lahc soft_med=1507 spread=6 gaps=1 quality 3/4. Two implementation shapes: (a) add new `ConstraintWeights` fields (`max_per_class_spread`, `max_per_class_interior_gaps`) penalising the worst class, plumbed through `score_solution`, the LAHC slice/canonical with incremental-delta arithmetic plus `debug_assert_eq!(state.canonical_score, score::score_solution(...))` per-iteration gate, CP-SAT's `_emit_objective` (per-class max over `add_max_equality` of per-day boolvars), and the `QualityReport` raw-count fields. (b) re-shape existing soft costs to penalise the worst class rather than the sum, breaking change against ADR 0029's reference data and the existing `BENCH_RESULTS.md` baselines. Path (a) is additive and preserves the existing comparison data; path (b) is a bigger refactor. Pick (a). Production-default ADR (item 47, ADR 0036) is allowed to land on per-component vectors that include the new worst-case axes; if item 47 ships before this item, it must explicitly call out worst_spread and worst-class interior gaps as not-yet-optimised quality bars and reference this item as the planned closure. Trigger: as soon as item 14 lands (Grundschule quality bar `xfail` removed), or earlier if item 47's ADR 0036 cites the multi-school quality miss as a deciding factor between backends. Out of scope until grundschule's quality bar is locked in.
58. **Surface `QualityReport` on the API response.** `[P1]` Today `Solution.soft_score: u32` is the only soft-cost signal on the wire format; `solver_core::QualityReport` (the per-axis cost-vector breakdown defined in `solver-core/src/quality.rs`) is computed for the bench but never serialized to the API. An admin or frontend cannot see "your soft score of 1500 decomposes into 800 home_room + 500 class_day_balance + 200 subject_pref" — the scalar is opaque. Plumb the breakdown through the boundary: (1) add a `QualityReport` Pydantic schema in `backend/src/klassenzeit_backend/scheduling/schemas.py` mirroring the Rust struct's raw-count fields plus `weighted_score`; (2) extend the solver-py binding (`solver/solver-py/src/lib.rs`) to return `(Solution, QualityReport)` from a new `solve_json_with_quality_report(...)` adapter or attach the report to the existing `Solution` JSON payload (pick whichever fits the existing `solve_json` shape better — the `Solution` attach is additive and avoids a new public function); (3) wire the report onto the `POST /api/classes/{id}/schedule` and `POST /api/schedule/all` response bodies; (4) regenerate frontend OpenAPI types via `mise run fe:types`; (5) optional follow-up: render the breakdown as a small badge stack on the schedule view ("home_room: 800, class_gap: 100, …"). Independent of items 14, 47, 57 — can ship anytime once the autopilot bench refresh PR merges. The contract `quality_report.weighted_score == solution.soft_score` is already pinned by `solver-core/tests/quality_property.rs`; the Pydantic shape inherits the same property. Trigger: when an admin demos a schedule and asks "why is the soft score 1500", or as soon as item 47's ADR 0036 reasons from per-component vectors and the same vectors should be visible to the frontend reviewer.
59. **Per-class / per-teacher attribution on `QualityReport`.** `[P1]` Land after items 57 and 58. Today `QualityReport.class_gap_hours: u32` is a sum across all classes; the same shape applies to `teacher_gap_hours`, `home_room_misses`, `class_day_balance_cost`. For "which class is the worst spread offender on dreizügig" debugging, the sums are insufficient; the admin needs maps. Add: `class_gap_hours_by_class: HashMap<SchoolClassId, u32>`, `teacher_gap_hours_by_teacher: HashMap<TeacherId, u32>`, `home_room_misses_by_class: HashMap<SchoolClassId, u32>`, `class_day_balance_cost_by_class: HashMap<SchoolClassId, u32>`. Property test `sum(map.values()) == legacy_field` per axis pins the consistency. Bench markdown rendering keeps the existing four sum columns (no row-shape change to BENCH_RESULTS.md); the API response (item 58) gains the maps so the frontend can render a "worst offenders" table. Once item 57 widens the search-time objective to penalise the worst class, the per-class attribution lets the admin see which class is the worst-case driver. Allocation cost: HashMap construction is post-solve only (cold path), runs once per `quality_report(...)` call, and `quality_report` is not in the LAHC hot loop — `solver/CLAUDE.md`'s "no allocation in `try_place_block`" rule is preserved. Trigger: same as item 58, or earlier if item 57's reviewer wants to validate the worst-class penalty actually targets the offender the maps report.
56. **Non-Timefold third-backend spike.** `[P2]` Trigger: a future BENCH_RESULTS refresh shows that Rust LAHC variants (post item 47's production-default revisit) and CP-SAT both plateau on the same quality axis. Candidate selection has been researched in `docs/research/2026-05-08-third-solver-backend-candidates/`; the report's priority order for the spike is (1) Pumpkin (Rust LCG-CP, TU Delft, dual Apache-2.0/MIT, pure-Rust integration via `cargo add pumpkin-solver`), (2) PySAT + RC2 / TT-Open-WBO-Inc MaxSAT (MIT, `klassenzeit_solver/cpsat.py`-shaped peer module), (3) good_lp + HiGHS or russcip + SCIP MIP (matheuristic plateau-breaker). Choco is excluded (Java); GLPK is excluded (GPL, last release December 2020). When the trigger fires, run a fresh brainstorm against those three candidates and produce ADR 0036+ accordingly. The research's central caveat: no published 2022-2026 paper documents a measurable win for HiGHS/SCIP/Gecode/Pumpkin over CP-SAT on a school-timetabling benchmark, so a no-third-backend ADR (analogous to ADR 0035 on Timefold) is a permissible outcome. Out of scope unless the trigger fires. Reuses the existing `BenchBackend` dispatch shape and the canonical `QualityReport` evaluator (item 50, ADR 0030).

### Independent follow-ups (P1, parallelizable)

16. **Wochenschema editor grid (P1).** Designed in PR #171's brainstorm Q9 but deferred. Replace `frontend/src/features/week-schemes/time-blocks-table.tsx` with a Tailwind grid mirroring `schedule-page-class-view.tsx`. Independent of the solver work; can ship in parallel with any active sprint.

### Drop tier (P2)

17. **Quality-issue endpoint.** `GET /api/schedule/quality-issues` returns `quality_checks.QualityIssue[]` for the current school's persisted schedule. Reuses `quality_checks.py`. Out of scope unless an admin demos the need.
18. **Multi-Wochenschema schools.** Existing Sprint B deferral, only revisit if a future fixture creates two Wochenschemata.
35. **Per-cell placement count distribution in the bake-off, not just median.** `[P2]` Bake-off bench reports median placement count per `(fixture, backend)` cell. Bimodal cells (50% of seeds drop placements, 50% do not) render cleanly in the median while half the runs are broken. Land only if item 29's refresh data shows a cell where `placements_total_median == placements_expected` while `feasibility_count < seeds`; the symptom is the median masking the reality. Mitigation: render an additional `min/max` per cell or a `feasibility-only median` when feasibility < seeds.
36. **Promote `placements_expected_for_problem` to `pub fn` in `solver-core`.** `[P2]` Today the helper lives in `solver/solver-bench/src/main.rs` because the bench is the only caller. If a Python harness in `klassenzeit_solver` (e.g. a CP-SAT-side validator) wants to double-check expected placement count without re-implementing the sum, promote the helper to `pub fn solver_core::placements_expected(problem: &Problem) -> u64` and bind through `solver-py`.

## Planned future sprints

Each sprint listed below ships its own brainstorm + spec + plan when it begins. Order reflects prereqs (Sek-I primitives before Sek II Kurssystem; both before fixture capstones). Tidy mixes are indicative; the sprint's brainstorm picks the actual quality items at the time.

### Sprint 2: Gymnasium Sek I fixture (G9, einzügig 5-10)

Real `demo_gymnasium_einzuegig` seed researched against the Hessisches Kultusministerium G9 Kontingent-Stundentafel for Klasse 5-10 ([Hessen Gymnasiale Mittelstufe](https://kultus.hessen.de/Schulsystem/Schulformen-und-Bildungsgaenge/Gymnasium/Gymnasiale-Mittelstufe)). First non-Grundschule fixture; exercises the school-type enum and a secondary-school subject set (Latein / Französisch as 2. Fremdsprache, NaWi, PoWi, Sport differenziert nach Geschlecht). Adds `SchoolClass.headcount: int | None` plus a pre-solve check "all rooms a lesson can land in must have `capacity >= school_class.headcount` (or null)". Tidy: per-worker postgres process for backend tests; structured-logging follow-up (b) (uvicorn access-log dedup).

### Sprint 3: Wahlpflichtfächer

New `WahlpflichtGroup` schema (a parent record naming the choice; per-Klasse rows recording which subject each Klasse picked from the group). Solver constraint: each Klasse picks exactly one Wahlpflicht subject per group; placements honour that pick. UI for Wahlpflicht setup. Real Klasse 8 / 9 / 10 Wahlpflicht fixture rows on the Sprint 2 Gymnasium fixture (3. Fremdsprache vs NaWi vs Informatik vs Kunst-AG, per Hessen G9 Mittelstufe lever). Tidy: TRUNCATE-based per-test reset; structured-logging (c) (request body / Content-Length).

### Sprint 4: Differenzierung (E-Kurs / G-Kurs)

`KursAufteilung` schema for Mathematik / Englisch / Deutsch differenziert (E-Kurs / G-Kurs) at Realschule and Gesamtschule. Solver constraint: a differenziertes Lesson-Paar shares its time-block (parallel split) but uses two teachers and two rooms. Full `demo_realschule` fixture researched against the Hessen Realschule Stundentafel ([Realschule Hessen](https://www.realschule-hessen.de/)). Tidy: coverage split (master-only CI job); structured-logging (d) (GCP / ECS field renames).

### Sprint 5: Sek II Kurssystem

The hardest schema pivot: Sek II introduces `Kurs` rows that have no fixed `SchoolClass` (Kurse cross Klassen) and per-Student `Kurswahl` rows recording which 2 LK + ~8 GK each student picks. New phase enum: `Einfuehrungsphase` (E1, E2) plus `Qualifikationsphase` (Q1, Q2, Q3, Q4). Klausuren-Fenster scheduling. Sek-II-shaped schedule view (per-Kurs and per-Schueler). Researched against the Hessen Kerncurriculum Oberstufe ([Kerncurriculum gymnasiale Oberstufe Mathematik](https://kultus.hessen.de/sites/kultus.hessen.de/files/2025-10/kerncurriculum_gymnasiale_oberstufe-mathematik.pdf)). Lands on its own because every assumption from "Lessons are per-Klasse" needs a parallel "Kurse are per-Kurs" track. Tidy: structured-logging (e) (CRUD route handler instrumentation); Doppelbesetzung / Foerderstunden two-teacher schema (`Lesson.co_teacher_id: uuid.UUID | None` plus solver "if set, co-teacher must also be free and qualified").

### Sprint 6: Gesamtschule full Sek-I fixture

Real `demo_gesamtschule` (24 classes Sek I, ~50 teachers, ~31 rooms, ~700 placements) using Wahlpflicht (Sprint 3) + Differenzierung (Sprint 4) primitives. Researched against a Hessen integrierte Gesamtschule (IGS) Stundentafel Sek I. First fixture that exercises both Sek-I primitives at scale; supersedes the long-standing OPEN_THINGS deferral. Tidy mix picked when the sprint brainstorm runs.

### Sprint 7: Gymnasium full G9 fixture (5-13)

Real `demo_gymnasium_g9_full` exercising Wahlpflicht (Sprint 3) + Sek II Kurssystem (Sprint 5) on one school. Capstone fixture: Klasse 5 Latein-Anfaenger to Q4 Leistungskurs-Klausuren, one researched dataset. Tidy mix picked when the sprint brainstorm runs.

## Reference data

### Hessen Grundschule reference data

Researched 2026-04-22 and mapped onto the schema during the seed brainstorm (the seed-design spec, since archived). Values below are the source figures; the seed reflects every "Yes" row and documents every "Not encoded" row against an existing OPEN_THINGS deferral.

- *Stundentafel Klasse 1/2:* 21 Pflichtstunden (Deutsch 6, Mathematik 5, Sachunterricht 2, Religion/Ethik 2, Kunst/Werken/Musik 3, Sport 3) plus 2 Stunden Förderunterricht/AGs.
- *Stundentafel Klasse 3/4:* 25 Pflichtstunden (Deutsch 5, Mathematik 5, Sachunterricht 4, Fremdsprache 2, Religion/Ethik 2, Kunst/Werken/Musik 4, Sport 3) plus 2 Stunden Förderunterricht/AGs. Über alle vier Jahrgänge also 92 Wochenstunden gesamt.
- *Lehrer-Pflichtstunden (Grundschule):* 28 Wochenstunden Vollzeit, ab 01.02.2026 reduziert auf 27,5. Teilzeit wird anteilig als Bruch geführt (typische Werte: 14/28, 18/28, 21/28). Lebensarbeitszeitkonto: 0,5 Stunden pro Woche Gutschrift bis zum 60. Lebensjahr (anteilig bei Teilzeit).
- *WeekScheme-Zeitraster:* Unterrichtsbeginn 7:45 bis 8:15, Unterrichtsstunde = 45 Minuten. Zwei Hofpausen (je 15 bis 20 Minuten, nach der 2. und nach der 4. Stunde) plus eine kurze Frühstückspause im Klassenraum (ca. 10 Minuten). Tagesende 11:30 bis 13:20 im Halbtag, bis 14:55 im Ganztag. Ganztagsschulen ergänzen eine Mittagspause von 45 bis 60 Minuten. The shipped einzuegig and zweizuegig seeds use a 6-period Halbtag grid (08:00 to 13:05); the dreizuegige Ganztagsschule seed extends to 8 periods (08:00 to 14:50) per the Ganztag pattern.
- *Quellen:* Hessisches Kultusministerium, Hessischer Bildungsserver, GEW Hessen Pflichtstundenverordnung.

## Acknowledged deferrals

Items deliberately out of scope of any active sprint, with rationale for when to revisit. Sprint PRs may touch them in passing; do not retrofit out-of-scope changes into a sprint PR.

- **Tighten Grundschule schedule quality bar.** Historical anchor for active item 14. PR `feat/solver-grundschule-constraints` shipped a `quality_checks` predicate module plus an integration test (`test_grundschule_schedule_quality_meets_quality_bar`) that runs against the demo Grundschule and asserts every predicate is empty. The test ships **xfail** with thresholds `max_spread=2`, `min_ratio=0.6`, `max_gaps_per_class=2`. Original observation (since closed by items 12 / 21 / 48 / 52 / 54): daily spreads up to 5, home-room hits as low as 0/17. Item 44's 2026-05-08 production refresh shows grundschule `worst_spread=1`, `worst_home_med=1.00`, `gaps=0` on every backend; the FFD-greedy + LAHC fix shape the original entry proposed has effectively shipped. Active follow-through is item 14 (drop the `xfail`).
- **Wochenschema editor: replace dialog-driven table with a grid editor.** Today `frontend/src/features/week-schemes/time-blocks-table.tsx` is a 335-line table that opens a Dialog for every add/edit/delete. Brainstorm Q9 of `feat/solver-grundschule-constraints` planned a grid editor mirroring the schedule view (columns = days, rows = period numbers, bulk add/remove row, per-cell add/edit/delete). Deferred to a follow-up PR to keep the solver-constraints PR reviewable. Pick this up after the quality-bar item above so admins can tune their period count without database surgery.
- **Repository / unit-of-work layer.** Routes currently take `AsyncSession` directly. The schedule handler loads 7 to 8 entity types in one call and duplicates some of the existing CRUD queries. The earlier guidance ("add when it hurts") still applies; if a handler grows past ~80 lines, file the pain as a follow-up rather than detouring mid-sprint.
- **`active` flag on WeekScheme.** Matters only if a school has multiple week schemes with one "live" scheme. For the prototype, a class points at one `week_scheme_id` directly, which is enough. Revisit when the readiness checklist or a schedule-switcher needs it.
- **Auto-infer WeekScheme time-block position + validate ordering.** UX polish that does not affect the solver or the schedule view. Today the time-blocks form asks users to type a period number, and nothing prevents overlapping or out-of-order periods on the same day. Change to: drop the `position` input; backend assigns `position` as the chronological rank (by `start_time`) among blocks on the same day, renumbering siblings in one transaction on insert/edit; validate that consecutive pairs on the same day sorted by `start_time` satisfy `start_time >= previous end_time`, returning 422 on overlap. Frontend mirrors the check and maps 422 to a form root error, following the existing 409-duplicate pattern. DB column and `(day, position)` uniqueness stay as-is. Reported during frontend small-fixups session.
- **Persist solver violations or surface them on GET.** Violations are returned by `POST /api/classes/{id}/schedule` but not persisted. `GET /api/classes/{id}/schedule` returns placements only. If the schedule view needs a persistent "why is this incomplete?" diagnostic, either add a `schedule_violations` table or teach the GET endpoint to re-run the solver and surface fresh violations on demand. Reported during placement-persistence work.
- **Advisory lock on concurrent POSTs for the same class.** Current guard is Postgres row-level locks inside the delete-then-insert transaction (last writer wins). An explicit `pg_advisory_xact_lock(hashtext(class_id::text))` would prevent wasted parallel solves when demo users click Generate twice. Add if traffic shows interleaving. Reported during placement-persistence work.
- **Multi-week-scheme support in teacher / room schedule views.** Sprint B's `schedule-page-teacher-view.tsx` and `schedule-page-room-view.tsx` build the time-block index from the first class's `WeekSchemeDetail` only (`classes.data?.[0]?.week_scheme_id`). For a school where teachers or rooms span Klassen on different week schemes, placements whose `time_block_id` doesn't appear in that scheme are silently dropped by the cell-builder filter. Mitigation: fetch every distinct `week_scheme_id` across `useSchoolClasses().data`, request each via `useWeekSchemeDetail`, and merge their `time_blocks` arrays into a single `Map`. Out of scope until a customer school runs more than one active week scheme; surfaced during the Sprint B implementation.
- **Drag-and-drop on teacher view and room view.** Sprint C ships drag-and-drop on the class view only. The teacher and room views render the same `ScheduleGrid` but their cell builders don't wire the `@dnd-kit/core` layer; pinning and moving from those views silently no-op. Wiring requires per-view DnD context plus a decision on what "move" means when the dragged placement spans multiple classes (the dragged row identifies a teacher's lesson, but the placement still belongs to N classes). Surfaced during Sprint C planning; revisit after a demo asks for it.
- **Cross-class swap from a unified view.** Today's swap endpoint (`POST /api/placements/swap`) is class-agnostic at the backend, but the only UI that drives it is the per-class view, so a user can only swap two placements that both touch the active class. A unified all-classes view would let a user swap placements across arbitrary class pairs; the existing endpoint already supports it. Defer until a demo asks. Surfaced during Sprint C planning.
- **Mobile touch UX for drag-and-drop.** `@dnd-kit/core` ships a `TouchSensor` but the Sprint C wiring uses `PointerSensor` only. On mobile this means a touch-drag can lose track of the pointer or interfere with scrolling. Long-press-to-activate via `TouchSensor` is the conventional fix; pair with a separate "pin" affordance (long-press menu) so a tap is unambiguously navigation. Out of scope until a demo runs on a tablet. Surfaced during Sprint C planning.
- **Soft / tentative pin semantic.** ADR 0028 makes the pin a hard constraint. A second `pinned_kind` column with a soft variant the solver scores against would let a user say "I prefer this placement, but reroute it if you must". Adds a score axis that needs per-fixture tuning, plus a UX surface (two badges instead of one). Out of scope until the workflow surfaces a real "preferred-not-required" case; revisit with its own ADR if it does. Surfaced during Sprint C ADR work (rejected alternative).
- **Pre-existing tsc TS2741 errors in MSW test fixtures.** Five pre-existing strict-mode errors where MSW response payloads in `frontend/src/lib/api/hooks.test.tsx`, `frontend/src/routes/__tests__/schedule-page.test.tsx`, and `frontend/src/routes/__tests__/schedule-toolbar.test.tsx` omit the `pinned` field on `ScheduledLesson` records. Vitest uses esbuild (lenient about omitted required fields) so the suite passes today; only strict `tsc --noEmit` flags them. Pre-date Sprint C but surfaced during Task 8. Tidy-first follow-up: a single pass adding `pinned: false` to the relevant fixtures across the three files. CI does not currently run `tsc --noEmit` separately; that should be added in lockstep with the fix.
- **`mise run e2e` leaves seed data in `klassenzeit_test`.** Subsequent `mise run test:py` runs hit unique-constraint violations on the seed entities the e2e fixture committed. Surfaced when Task 10 of Sprint C ran the e2e suite mid-implementation. The Sprint A `db.commit()` for `seed-grundschule` is deliberate (the e2e fixture relies on it); the Sprint C commit-on-write fix (commit 7542d6f) made the leak more visible because more endpoints now persist. Two viable fixes: (a) `mise run e2e` truncates non-preserved tables on exit, or (b) the pytest conftest `TRUNCATE`s the test DB before applying migrations on its first run. Pick whichever fits the broader test-isolation overhaul.
- **`db.commit()` audit across all routes.** Sprint C added `await db.commit()` + `await db.refresh(...)` to the three placement endpoints and retroactively fixed the same omission in `POST /api/classes/{id}/schedule` and `POST /api/schedule/all` (commit 7542d6f). `lessons.py` and `rooms.py` were spot-checked and look correct, but no full sweep happened. Worth a one-PR sweep over every `routes/` module to confirm nothing else silently rolls back. The bug class is "tests pass because `expire_on_commit=False` keeps the in-memory object usable, but the row never lands"; it does not surface until an e2e test or a real client hits the endpoint. Surfaced during Sprint C Task 8.
- **Cross-class merged Religion groups beyond a single Jahrgang.** Picked up in the active "Realer Schulalltag" sprint at the per-Jahrgang level (one Rel-kath / Rel-ev / Ethik trio per Jahrgang spans the three Klassen of that grade). Real Hessen schools sometimes merge across Jahrgänge too (1+2 share a Religion group, 3+4 share another) when small-school staffing demands it. Defer the cross-Jahrgang case until a customer school surfaces the pattern; the per-Jahrgang case in the active sprint already exercises the multi-class lesson schema and the lesson-group co-placement constraint, so cross-Jahrgang is an additional fixture row, not a new schema change.
- **Pausen- / Aufsichtspflichten and Vertretungsreserve.** Teachers owe duty-minutes during Hofpausen on a rota; substitution reserve reduces their teaching capacity below `max_hours_per_week`. The schema has no break metadata on TimeBlocks (breaks are implicit gaps) and no reserve budget on Teacher. Two separate follow-ups: (a) `TimeBlock.kind` enum or a sibling `Break` table so supervision can be scheduled; (b) `Teacher.reserve_hours_per_week` with the solver subtracting it from the max. Out of scope until a customer school tracks supervision. Surfaced during grundschule seed research.
- **Teilzeit-Tage patterns on Teacher.** Part-time teachers are often contracted to specific weekdays (e.g. Dana works Mo, Di, Mi). Today this is representable via `teacher_availabilities` rows marking all time-blocks on off-days as unavailable, but that is verbose (15 rows per off-day) and the seed leaves availability empty. A `Teacher.working_days: set[int]` column or a higher-level "weekly pattern" model would let users describe "half-time, Mo-Mi" in one field. Revisit when a second real Teilzeit teacher enters demo data and the verbose availability grid starts to feel painful. Surfaced during grundschule seed research.
- **Lebensarbeitszeitkonto (teacher seniority time credit).** Hessen GEW Pflichtstundenverordnung: teachers earn 0.5h/week into a long-service time account until age 60, anteilig für Teilzeit. Not representable today (no age or seniority metadata on Teacher; no time-account model). Low priority since the credit is a payroll concept, not a scheduling hard constraint; document the deferral so future users asking "why isn't 27.5 in the seed?" see the answer. Surfaced during grundschule seed research.
- **Synthetic bench fixture generator (sprint item 6, optional leg).** A parametric `gen_synthetic_problem(num_classes, num_teachers, num_subjects, num_rooms)` producing a deterministic `Problem` JSON for stress tests at 100+ classes. Originally optional in the OPEN_THINGS item 6 description; deferred from the zweizügige PR. Revisit when items 7 to 9 surface a concrete need that the named-fixture matrix (grundschule + zweizuegig + gesamtschule once that lands) cannot answer.
- **Subjects table column for preference flags.** A check-mark or icon column next to each subject in the list view. Skipped in PR-9c because two boolean columns clutter the existing four-column row; revisit when users say they cannot see at-a-glance which subjects are flagged. Surfaced during PR-9c.
- **School-classes table column for home room.** A column showing each class's home-room name on the list view. Skipped in the home-room PR because the list table already carries Name, Grade, Curriculum, Week scheme, Actions; a sixth column clutters more than it helps for the prototype. Revisit when users say they cannot see at-a-glance which Klassenraum a class is pinned to. Surfaced during the home-room preference PR.
- **Per-class subject preference overrides.** A class wanting "Sport last period despite the global avoid-first flag" needs a `school_class_subject_preferences` table. Out of scope for the active "Realer Schulalltag" sprint (the global per-Subject flag plus the Jahrgang-level home-room mapping cover the prototype's pattern); revisit once a school surfaces a per-class deviation. Surfaced during PR-9c.
- **Configurable LAHC deadline (user-facing query param).** `?deadline_ms=<n>` query parameter on `POST /api/classes/{id}/schedule`. The test-path leg of this deferral closed in PR-2 (2026-04-30): `KZ_SOLVE_DEADLINE_MS` env var now plumbs through `Settings` to `run_solve`, and `solve_json_with_config(deadline_ms)` exposes the same control on the binding side. ADR 0033 (item 33) raised the production default to 5000 ms; the user-facing query parameter is still deferred. File when a demo user asks for control over solver wall-clock, when staging surfaces a per-school latency budget that 5000 ms breaches, or when item 34 ships `solve_deadline_ms_by_backend` and per-backend overrides become the natural next surface to user-configure. Surfaced during PR-9b.
- **Promote `SolveConfig.max_iterations` to a production knob.** Today the field exists for property-test determinism only; production callers leave it `None`. Add a wire-format pass-through (FastAPI query param + Pydantic field on the request body) if iteration-bounded solves become a real use case (e.g., a "preview the next move" UX or an A/B test where wall-clock variance matters). Surfaced during PR-9b.
- **LAHC telemetry on `Solution`.** Add `iterations: u64`, `accepted: u64`, `rejected: u64` fields once production observability needs them. Bench harness can print these via direct internal call, but the wire format change is wider than the property tests need today. Tracks alongside the existing follow-up to extend `solver.solve.done` log with `soft_score_before` / `soft_score_after`. Surfaced during PR-9b.
- **Block-aware FFD eligibility.** `ordering::ffd_order` ranks lessons by `free-teacher-blocks * suitable-rooms`, ignoring contiguity. A length-2 lesson with the same eligibility as a length-1 lesson is *more* constrained because it needs a contiguous run. Folding contiguity into the eligibility computation requires precomputing per-(teacher, day) free-position runs of length `>= n`, which adds state. Revisit if a fixture surfaces a real placement-rate regression on block-heavy schedules. Surfaced during PR-8 (Doppelstunden).
- **Block-aware LAHC Change move.** Today's LAHC skips block placements entirely (one-line guard inside `try_change_move`). A block-aware Change move would pick a block placement and try to relocate the whole `n`-window to a new contiguous start; the property test in `tests/lahc_property.rs` would need a third RNG draw per iteration to pick the start position, so the determinism RNG-budget invariant changes shape. Out of scope until benches surface a real soft-score gap on block-heavy fixtures. Surfaced during PR-8.
- **Block-aware Swap move.** Today's LAHC has no Swap move at all. A natural extension once Swap exists: swap two same-`n` blocks atomically. Tracks alongside the LAHC Change-move follow-up. Surfaced during PR-8.
- **Atomic group-swap LAHC move.** Today's LAHC skips lesson-group placements via a one-line guard in `try_change_move` (mirrors the Doppelstunden pattern). A richer move shape would pick a group, move every member to a new TB, and reassign rooms greedily; the determinism property test in `tests/lahc_property.rs` would need a third `random_range` draw per iteration. Out of scope until benches surface a real soft-score gap on group-heavy fixtures. Surfaced during the lesson-group co-placement PR.
- **Surface `lesson_group_id` in lesson edit dialog.** The multi-class refactor PR ships `lesson_group_id` end-to-end (Pydantic, ORM, OpenAPI, solver wire format) but the lesson edit dialog doesn't read or write it. Add a read-only "Gruppe X" badge to the lesson row first; an editable group dropdown comes later when the lesson-group co-placement constraint exists and an admin can usefully edit it. Surfaced during the multi-class lessons PR.
- **Block-aware LAHC for cross-class lessons.** The LAHC Change move generalises naturally over `school_class_ids` (the multi-class lessons PR ships that), but the lesson-group constraint adds a "swap the whole group atomically" move shape. Tracks alongside the existing `Block-aware LAHC Change move` Doppelstunden follow-up. Surfaced during the multi-class lessons PR.
- **Visual merge of adjacent same-lesson cells in the schedule grid.** `frontend/src/routes/schedule.tsx` renders one cell per `Placement` with no cross-cell awareness. Two adjacent same-lesson cells convey "Doppelstunde" pedagogically but a `rowspan`-merged single cell with one label is the polish. Requires teaching the grid renderer about lesson identity across cells. Surfaced during PR-8.
- **Mixed block sizes inside one lesson.** Today `validate_structural` requires `h % n == 0`. Allowing `h=3, n=2` to mean "one Doppel plus one Einzel" needs per-hour metadata about which hour was the remainder; the violation taxonomy and the schedule view both leak the distinction. Users currently model the two shapes as two separate Lessons. Surfaced during PR-8.
- **`preferred_block_size > 2`.** Pydantic constrains `le=2`. The Rust algorithm in `try_place_block` is generic in `n`, but the API caps it at 2 until a real ask surfaces (e.g., `n=3` for Sport am Nachmittag in three consecutive hours). Surfaced during PR-8.
- **Coverage split (master-only job).** OPEN_THINGS option (b) from the original CI-slowness entry. Move coverage instrumentation into a master-only CI job so PR runs use bare pytest. After PR-1 shipped, the Test job is still ~21 min on a representative PR (xdist parallelism alone produced ~zero speedup because Postgres is the bottleneck, not CPU). Splitting coverage off would let PR runs use bare pytest at ~7-10 min while master's commit-after-merge run keeps the ratchet. Surfaced during PR-1; this is the highest-leverage next lever.
- **Investigate the Postgres bottleneck (remaining legs).** PR-2 (2026-04-30) shipped option (3): an Alembic-migrated template DB cached via `CREATE DATABASE ... TEMPLATE`, so per-worker DBs skip the per-process migration cost. The two heavier-lift legs remain deferred and are tracked under "CI / repo automation" below: (1) per-worker `pytest-postgresql` instances (each worker gets its own postgres process), (2) `TRUNCATE`-based reset replacing per-test transaction-rollback isolation. Surfaced during PR-1.
- **Prune slowest seed solvability tests.** OPEN_THINGS option (c) from the original CI-slowness entry. The seed solvability tests (`test_demo_*_solvability.py`) boot the solver per fixture; they are likely the long-tail tests that dominate any single worker's runtime. Replace solver-boots with cached fixtures where the solver behaviour is not under test. Surfaced during PR-1.

## Backlog

Everything below is queued for later. Ordered roughly by importance within each group.

### Product capabilities

- **Deep-linked entity edit.** The Dashboard "Recently edited" tile links to the entity's list page without opening the edit dialog for that row. Add a `?edit=<id>` search param (validated by Zod in `validateSearch`) on each CRUD page, and teach the list component to open the matching dialog on mount. Defer until a second use case demands bookmarkable edits.
- **Teacher-centric schedule view.** Today `/schedule` shows one class at a time. A teacher-centric view answers "where is Frau Müller all week" and needs either a new `GET /api/teachers/{id}/schedule` endpoint or frontend aggregation across all classes a teacher has qualifications for. Ship after a demo reveals the need; the per-class view is enough for the prototype. Surfaced during the schedule-view PR.
- **Room-centric schedule view.** Mirror of the teacher view; "what happens in Room 101 all week". Same trade-off, same follow-up timing. Surfaced during the schedule-view PR.
- **Duplicate a Stundentafel.** Creating a variant means re-adding every subject entry by hand, which is tedious. Add `POST /stundentafeln/{id}/duplicate` that clones the tafel plus all entries in one transaction, auto-suffixing the name (`"{orig} (Kopie)"`, then `"(Kopie 2)"`, `"(Kopie 3)"` on collision). Frontend: a "Duplizieren" / "Duplicate" button in the row's action cell next to Edit/Delete; on success toast + navigate to edit the new tafel. Reported during frontend small-fixups session.
- **Collapse the loading / error / empty + Toolbar wrapper too.** Now that `EntityListTable` and `EntityPageHead` ship, the only remaining duplication across the seven entity pages is the conditional `isLoading ? <p>…</p> : isError ? <p>…</p> : showEmpty ? <EmptyState/> : (<><Toolbar/><EntityListTable/></>)` block. A second primitive (`<EntityListShell loading error empty toolbar>{children}</EntityListShell>` or similar) would land cross-entity polish for those states once instead of seven times. Skipped in the EntityListTable PR because (a) the body shape varies (six tables vs WeekSchemes panel) and forcing a common signature now risks over-abstracting, and (b) the value/cost ratio is weaker than the table itself. Revisit once a polish pass on loading or empty states surfaces it as a pain point. Surfaced during the EntityListTable refactor.
- **Sub-resource setup in the create flow.** Today "New teacher" saves the bare entity, then the user must click Edit to add qualifications; same pattern for rooms (suitability) and week-schemes (time blocks). It's a two-step workflow for what feels like one action. Options: (a) auto-reopen the edit dialog with the new entity after a successful create, so sub-resource editors are the natural next step; (b) inline the sub-resource editors inside the create dialog and defer their POST until after the parent save succeeds (non-atomic, extra error-handling for partial failures). Option (a) is cheaper; prefer unless a user study says the extra click still hurts. Constraint: keep the memory rule that sub-resource editors live inline in the parent dialog, not on separate routes. Reported during frontend small-fixups session.
- **Bulk delete across entity tables.** Design includes checkbox columns; we dropped them this pass because there's no bulk-delete backend route. Add `DELETE /<entity>?ids=...` + a confirm dialog once there's a compelling workflow.
- **Import / export buttons.** Placeholder "Import" button renders disabled on every CRUD page. Wire to backend CSV/JSON endpoints once those land.
- **Route RHF root errors through toasts.** With `sonner` landed, the next cross-entity pass can consider replacing `form.setError("root", ...)` on 409-on-delete with a `toast.error(...)` surface. Intersects with "Typed deletion errors for in-use entities"; decide UX in that PR.
- **Dedicated `toasts.*` i18n namespace.** Today the only toast copy reuses `schoolClasses.generateLessons.*`. When a second, non-schoolClasses feature fires a toast, carve out a `toasts.*` namespace in that same pass rather than scattering toast strings across entity namespaces.
- **Typed deletion errors for in-use entities.** Deleting a Room or Teacher that a Lesson references surfaces the backend 409 as a generic `ApiError` toast. A typed 409 handler, or a pre-flight "is-used" check before opening the delete dialog, should land as one cross-entity pass rather than per-entity duplication.
- **`entry_count` / `total_hours` on `StundentafelListResponse`.** The Stundentafel list row shows Name, Grade, Actions only. A total-hours or entry-count column would be nice but requires a backend schema change; defer until users ask for an at-a-glance signal.
- **Translate Zod validation errors beyond login.** `LoginSchema` reads message keys via `i18n.t()` at module load (so the text is whatever language was detected on first load and does not update on locale switch). Subjects, Rooms, Teachers, and WeekSchemes schemas all ship with raw English literals. Ship a translated Zod global error map once a second non-login form surfaces them.
- **Raise the frontend coverage floor.** Ratchet currently floors at 50% with baseline 61%. Bump the floor to 70% once baseline clears 75% organically, then 80% to match Python.
- **Parallel `mise run dev` for backend + frontend.** Currently needs two terminals. A `concurrently`-style task or a `mise run dev:all` task would be convenient.
- **Self-hosted fonts.** Frontend imports Quicksand / Lora / Fira Code / Special Elite via `@import url(fonts.googleapis.com/...)`. Move to locally hosted `@font-face` (`public/fonts/*.woff2`) once offline dev or third-party privacy is a concern.
- **Time-of-day-aware welcome greeting.** Dashboard shows "Welcome back." regardless of clock; prototype suggested "Guten Morgen, Pascal." A one-liner with `Intl.DateTimeFormat` plus the logged-in user's first name.
- **Untranslated-string lint rule.** Review discipline is the only line of defence against hardcoded English or German sneaking into JSX. Add a Biome plugin or parallel ESLint rule if violations happen in practice.
- **Production deployment.** Docker, reverse proxy, secrets management. Staging already auto-deploys via `.github/workflows/deploy-images.yml`; this item is the prod pathway specifically.
- **Data migrations / seed data framework.** Schema migrations only for now. Add when there's real data to seed beyond the prototype's one-shot `demo_grundschule` script.
- **MFA / TOTP / passkeys.** Not needed for current threat model. Add if user base or sensitivity grows.
- **Email-based password reset.** Requires email sending infrastructure. Add when email is needed for other features.
- **OAuth / OIDC / social login.** Not needed for closed system.
- **Self-service registration.** Not needed for closed system.
- **Bulk import/export.** CSV or JSON import for teachers, rooms, subjects. Useful but not needed for MVP.
- **WCAG AA contrast for light-mode primary/secondary buttons.** `@google/design.md lint frontend/DESIGN.md` surfaces `contrast-ratio` warnings on `button-primary` (`#608c5e` on `#ffffff` = 3.88:1, below 4.5:1 AA; passes AA for large text at 3:1) and `button-secondary` (`#7ba8bc` on `#ffffff` = 2.57:1, below AA at any size). Fix options: darken the moss/blue primaries, switch to a dark textColor on those buttons, or restrict them to large-text usage. Decide when light-mode accessibility audit is on the roadmap; until then the warnings stay visible in every `mise run lint` run. Surfaced during DESIGN.md adoption.
- **Auto-assign-teachers count in the "Generate lessons" toast.** The `POST /api/classes/{id}/generate-lessons` response now contains a `teacher` object on each pinned lesson. The success toast still reads `N lessons created`; a follow-on `M teachers auto-assigned` line (or interpolated copy) would surface the heuristic's outcome to admins, including the case where some lessons stayed null because no qualified teacher had capacity. Needs i18n keys in en + de and a count derived from the response. Skipped in the auto-assign-teachers PR because it is UX polish on top of the wire change. Surfaced during the auto-assign-teachers PR.

### CI / repo automation

- **Re-tighten `.test-duration-budget`** after later changes shift the floor. Current ceiling is 120 s against an observed master-run floor of ~25 s (set 2026-05-02 in PR `chore/tighten-test-duration-budget`). When a future PR-2-class wall-clock drop lands, repeat the ratchet: wait for two or three master runs at the new floor, then lower the budget to roughly 4 to 5x the new max in a single-line PR.
- **Per-worker postgres process for backend tests.** Originally option (1) of the "Investigate the Postgres bottleneck" deferral. All xdist workers currently share one Postgres service container; switching to per-worker `pytest-postgresql` instances (each worker gets its own postgres process) would address the I/O contention diagnosed in PR-1, though the contention is no longer the dominant cost after PR-2 dropped the suite from 20 min to 27 s. Lower priority now; revisit if the suite grows back into a problem.
- **`TRUNCATE`-based per-test reset for backend tests.** Originally option (2) of the "Investigate the Postgres bottleneck" deferral. Drop the per-test transaction-rollback isolation in favour of a `TRUNCATE` on dirty tables; faster than savepoint setup/teardown for short DB-heavy tests. Lower priority than per-worker postgres, but cheaper to prototype. Surfaced during PR-1.
- **Coverage split (master-only job).** Move coverage instrumentation into a master-only CI job so PR runs use bare pytest. Was the highest-leverage next lever before PR-2 dropped the suite to 27 s; now mostly redundant unless coverage runs become slow again.
- **Dependabot for the Python/uv ecosystem.** Dependabot doesn't natively understand `uv.lock` as of mid-2025; the `pip` adapter desyncs the lockfile and violates the `uv add`-only rule. Revisit when dependabot ships first-class uv support, or switch to Renovate (which already supports uv).

### Testing

- **Session-scoped event loop may cause timing interference at scale.** The `asyncio_default_fixture_loop_scope = "session"` setting (introduced for the Chunk G DB fixtures) means all async tests and fixtures share a single event loop for the entire pytest session. This prevents asyncpg "Future attached to a different loop" errors with session-scoped fixtures but means one slow or stalled async test can delay all subsequent tests in the session. Not a problem with the current 16-test suite, but worth revisiting if the test suite grows large or if tests with long async timeouts are added.

#### E2E (Playwright)

- **Entity coverage beyond Subjects.** Each remaining entity CRUD spec (Rooms, Teachers, WeekSchemes, SchoolClasses, Stundentafel, Lesson) should add its own Playwright flow when it lands. The prototype sprint added one end-to-end schedule flow; per-entity specs remain deferred. Sprint PR 5 (`EntityListTable` primitive) unblocks a single-pass rollout.
- **Cross-browser matrix.** Firefox and WebKit are disabled for now (Chromium only). Enable when external users appear.
- **Accessibility audits inside Playwright.** `@axe-core/playwright` integration is deferred; track separately.
- **Visual regression, remaining approaches.** Approach 2 (computed-style diff across interaction states) shipped in `frontend/e2e/flows/computed-style-diff.spec.ts` and its supporting module; zero drift on current master. Two approaches remain for when design churn slows:
  1. **Pixel-diff snapshots** (Playwright `toHaveScreenshot`, Percy, Chromatic). Catches any pixel change, noisy on intentional design tweaks. Fallback if design stabilises enough to make a baseline meaningful.
  3. **Vision-LLM diff.** Crop before/after screenshots of the same element, pass both to a VLM with "do these shapes match, ignoring color and text". Fuzzier, catches emergent layout differences that don't trace to a single CSS property (pseudo-element sizing, child layout shift). Use as a follow-up when the computed-style diff says styles match but something still looks off.
- **Parallel workers + per-worker DBs.** Currently Playwright runs single-worker against a shared DB. Move to per-worker schemas once CI time matters.
- **Session cleanup in `/__test__/reset`.** The reset endpoint preserves the `sessions` table so storageState stays valid; revisit if tests start needing clean session state.
- **Nightly extended run.** Slower flows, broader data scenarios. Add when the suite is large enough to justify tiering.
- **Test-only router hardening.** Currently gated by `settings.env == "test"`; an additional network-level guard (e.g., bind `/__test__` to localhost only) is possible if the surface grows.
- **Integration test for conditional mount.** `include_testing_router_if_enabled` has unit tests but no integration test that actually imports `main` with `KZ_ENV=dev` and asserts `/__test__/*` returns 404. Add if a future refactor risks breaking the wiring silently.
- **Shell-exported `KZ_ENV=dev` defeats pytest router mounting.** The `os.environ.setdefault` in conftest no-ops if the shell already has `KZ_ENV` set. A shell-exported `KZ_ENV=dev` would silently skip mounting the testing router, and router tests would fail with 404. Add a warning in conftest or switch to `pytest-env` if this bites anyone.
- **Admin email must not use `.local` TLD.** `email-validator` (used by `pydantic.EmailStr`) rejects reserved domains. The seed admin uses `admin@example.com`. Revisit if we ever want a more realistic test domain.
- **Branch-protection required check and `e2e-gate` aggregator job.** The spec called for an `if: always()` aggregator that makes `e2e` a required check compatible with path-filtered skips. Not implemented; `e2e` currently runs only when paths match and is not listed in `.github/branch-protection.json`. Add both once the suite proves stable enough to block merges.
- **`TRUNCATE ... RESTART IDENTITY CASCADE` may reset sequences beyond the savepoint.** `RESTART IDENTITY` is DDL in some Postgres configurations and can bypass the per-test savepoint rollback. Not an issue at current suite size; revisit if tests begin relying on predictable sequence values.

### Toolchain & build friction

- **`ty` preview status.** Astral's type checker is pre-1.0; spec uses it anyway to keep the Python toolchain Astral-consistent. Revisit if it proves unstable.
- **`pytest-postgresql` or `testcontainers-python` as an alternative to compose-based test infra.** Revisit if onboarding friction emerges.
- **Structured logging follow-ups.** From `feat/structured-logging-backend` (ADR 0016): (a) replace or mute uvicorn's `--access-log` so production emits one access line per request, not two; (b) request / response body or `Content-Length` logging (needs PII review); (c) GCP / ECS / CloudWatch field renames once a log shipper is wired; (d) `solver-py` Rust-side structured logging once the Rust worker is real; (e) instrumenting CRUD route handlers on success and error. JSON formatter + `http.request` middleware shipped 2026-04-28; remaining items deferred from the DX/CI sprint with no firm owner. Item (a) (`contextvars`-based request_id propagation) shipped 2026-05-02 in `feat/request-id-contextvar`; siblings renumbered.
- **Split `frontend/CLAUDE.md` into topic files under `.claude/rules/`.** The frontend file is currently 127 lines. When it exceeds ~150 lines or starts topic-mixing, split into `.claude/rules/frontend-hooks.md`, `frontend-forms.md`, `frontend-testing.md`, etc. with `paths: ["frontend/**/*"]` frontmatter. Not yet warranted for a project this size.

### Auth maintenance

- **Session cleanup cron.** `mise run auth:cleanup-sessions` exists as manual task. Automate via cron or background scheduler when session volume justifies it.
- **Per-IP rate limiting.** Defer to reverse proxy (Caddy) or external service. Current limiter is per-email only.
- **Password breach check (HIBP).** Offline blocklist is the baseline. Online k-anonymity check against HIBP API is a nice-to-have.
- **Audit log.** `last_login_at` is the only tracking. Full audit trail is a separate concern.

### Production readiness

- **Multi-school tenancy.** The schema today has no `School` table and no `school_id` (or `owner_id`) on any aggregate root. `User` carries only auth fields; route handlers inject `_admin: User` purely for authn / role gating and do not scope queries. Every authenticated user therefore sees and edits the same global pool of teachers, rooms, classes, stundentafel, week schemes, lessons, and placements, so a single deployment can host exactly one school. Multi-tenant work is a coordinated change across (a) a new `schools` table, (b) a `school_id` FK on every aggregate root and on every per-school join row (lessons, placements, availabilities, suitabilities, qualifications), (c) a `school_id` on `User` (or a `school_users` membership table if cross-school admins are real), (d) query scoping in every route handler plus the solver wire-format build (`scheduling/solver_io.py:build_problem_json`), (e) seed scripts and demo fixtures, (f) E2E and integration tests. Revisit before onboarding a second customer school, or before any public / SaaS launch. Until then, single-tenant per deployment is the contract; the staging compose stack is one school, prod will be one school, and a second school means a second deployment.
- **Production DB configuration.** Connection pooling at scale, read replicas, `statement_timeout`, `pg_stat_statements`. All prod concerns, out of scope until the deployment spec.
- **Move Postgres init-SQL source into server-infra.** `server-infra/docker-compose.yml` mounts `/home/pascal/Code/Klassenzeit/docker/postgres/init-databases.sql` via an absolute host path, coupling the two repos by path rather than by contract. Move the file into the server-infra tree and update the mount source. Priority: low (only affects cold VPS setups).

### Project metadata

- **License.** Deferred. No `license` field in `Cargo.toml`, no `LICENSE` file. Revisit when the project's distribution model (open source vs proprietary vs SaaS) is clearer.

### User Tooling

Chat interface with Agent that can help with creations, modifications and deletions of entities. As well as see problems with the schedule and suggest fixes.

And of course in general help with the application.
