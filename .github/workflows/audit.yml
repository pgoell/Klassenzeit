name: Audit

on:
  schedule:
    # Mondays 06:17 UTC. The :17 minute is deliberate — :00 hits
    # GitHub's hourly cron stampede and gets queued.
    - cron: '17 6 * * 1'
  pull_request:
    paths:
      - 'Cargo.lock'
      - 'uv.lock'
      - 'deny.toml'
      - 'pyproject.toml'
      - '**/pyproject.toml'
  push:
    branches: [master]
    paths:
      - 'Cargo.lock'
      - 'uv.lock'
      - 'deny.toml'
  workflow_dispatch:

concurrency:
  group: audit-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  audit:
    name: Supply-chain audit
    runs-on: ubuntu-latest
    # The failure-path steps below open or reuse an issue titled
    # "CI: weekly supply-chain audit failing". That title is the dedup
    # key for `JasonEtco/create-an-issue@v2` with `update_existing: true`.
    # Do not change it in this workflow or in
    # .github/audit-failure-issue.md without a migration plan.
    permissions:
      contents: read
      issues: write
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-mise
      - name: Run mise audit pipeline
        run: mise run audit

      - name: Open or reuse tracking issue
        id: tracking-issue
        if: ${{ failure() && github.event_name == 'schedule' }}
        uses: JasonEtco/create-an-issue@1b14a70e4d8dc185e5cc76d3bec9eab20257b2c5 # v2.9.2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          RUN_NUMBER: ${{ github.run_number }}
          RUN_ATTEMPT: ${{ github.run_attempt }}
          TRIGGER_EVENT: ${{ github.event_name }}
        with:
          filename: .github/audit-failure-issue.md
          update_existing: true

      - name: Append run link as comment
        if: ${{ failure() && github.event_name == 'schedule' && steps.tracking-issue.outputs.number != '' }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ steps.tracking-issue.outputs.number }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          timestamp="$(date -u '+%Y-%m-%d %H:%M UTC')"
          gh issue comment "$ISSUE_NUMBER" \
            --body "Scheduled audit failed again on ${timestamp}. Run: ${RUN_URL}"

  drift-check:
    name: GitHub repo settings drift
    runs-on: ubuntu-latest
    if: ${{ github.event_name != 'pull_request' }}
    permissions:
      contents: read
      issues: write
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-mise
      # Reading branch protection requires the "Administration" (read) repository
      # permission. The Actions GITHUB_TOKEN cannot be granted that scope (it is
      # not one of the workflow `permissions:` keys), so this check needs a
      # separate token: a fine-grained PAT with Administration:read + Contents:read,
      # or a classic PAT with the `repo` scope, stored as the REPO_ADMIN_TOKEN
      # secret. When the secret is absent the check skips (green) with a notice
      # rather than failing with a 403.
      - name: Check repo settings drift
        env:
          GH_TOKEN: ${{ secrets.REPO_ADMIN_TOKEN }}
        run: |
          if [ -z "${GH_TOKEN}" ]; then
            echo "::notice title=drift-check skipped::REPO_ADMIN_TOKEN is not configured, so the branch-protection drift-check was skipped. Reading branch protection needs an Administration:read token, which the default GITHUB_TOKEN cannot hold. Add a fine-grained PAT (Administration:read + Contents:read) or a classic PAT (repo scope) as the REPO_ADMIN_TOKEN secret to enable it."
            exit 0
          fi
          bash scripts/apply-github-settings.sh --check

      - name: Open or reuse tracking issue
        id: tracking-issue
        if: ${{ failure() && github.event_name == 'schedule' }}
        uses: JasonEtco/create-an-issue@1b14a70e4d8dc185e5cc76d3bec9eab20257b2c5 # v2.9.2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          RUN_NUMBER: ${{ github.run_number }}
          RUN_ATTEMPT: ${{ github.run_attempt }}
          TRIGGER_EVENT: ${{ github.event_name }}
        with:
          filename: .github/drift-check-failure-issue.md
          update_existing: true

      - name: Append run link as comment
        if: ${{ failure() && github.event_name == 'schedule' && steps.tracking-issue.outputs.number != '' }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ steps.tracking-issue.outputs.number }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          timestamp="$(date -u '+%Y-%m-%d %H:%M UTC')"
          gh issue comment "$ISSUE_NUMBER" \
            --body "Scheduled drift-check failed again on ${timestamp}. Run: ${RUN_URL}"
